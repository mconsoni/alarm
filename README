Steps to use the alarm:

1. Create virtual environment
	# python3 -m venv venv
	# source venv/bin/activate
	# pip install -r requirements.txt

2. Use: 
	# ./pyAlarm.py <ALARM_TIME>
		ALARM_TIME format: HH:MM:SS (24hs format)
